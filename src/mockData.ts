import { Friend, DoorNote, DoorInteraction } from './types';

// Live production mode — no test/mock contacts, notes, or events
export const INITIAL_FRIENDS: Friend[] = [];

export const INITIAL_DOOR_NOTES: DoorNote[] = [];

export const INITIAL_DOOR_INTERACTIONS: DoorInteraction[] = [];
